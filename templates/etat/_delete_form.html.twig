<form method="post" action="{{ path('app_etat_delete', {'id': etat.id}) }}" onsubmit="return confirm('Voulez vous supprimer cet etat?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etat.id) }}">
    <button class="btn">Supprimer</button>
</form>
