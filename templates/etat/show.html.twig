{% extends 'base.html.twig' %}

{% block title %}Etat{% endblock %}

{% block body %}
    <h1>Affichage de l'etat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etat.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ etat.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_etat_index') }}">Retourner à la liste</a>

    <a href="{{ path('app_etat_edit', {'id': etat.id}) }}">Modifier</a>

    {{ include('etat/_delete_form.html.twig') }}
{% endblock %}
